<h1><%= @user.name %>'s Dashboard'</h1>

<%= button_to 'Discover Movies', user_movies_path(@user.id) %>

<h3>Viewing Parties</h3>
<% @user.view_parties.each do |vp| %>
  <div id="<%= vp.id %>">
    <p>Date: <%= vp.start_date %></p>
    <p>Time: <%= vp.start_time %></p>
  </div>
<% end %>
